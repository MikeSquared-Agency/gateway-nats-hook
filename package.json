{
  "name": "gateway-nats-hook",
  "version": "1.0.0",
  "description": "OpenClaw workspace hook that publishes session transcript chunks to NATS",
  "main": "handler.ts",
  "scripts": {
    "test": "node --import tsx --test --test-force-exit handler.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nats": "^2.29.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.19.3",
    "typescript": "^5.9.3"
  }
}
